--!strict

local Config = {
	MIN_PLAYERS = 4,
	MAX_PLAYERS = 10,
	SELECTION_DURATION = 10,
	GUESSING_DURATION = 30,
	DISCUSSION_DURATION = 15,
	VOTING_DURATION = 20,
	OVERTIME_DURATION = 10,
	OUTCOME_DURATION = 8,
	AFK_TIMEOUT = 120,
	MAX_ROUNDS = 3,
	INVESTIGATOR_WIN_SCORE = 100,
	IMPOSTER_WIN_SCORE = 200,
	VOTE_BONUS_SCORE = 25,
	MAX_HINT_LENGTH = 24,
	COINS_PER_WIN = 50,
	INPUT_THROTTLE = 0.5,
}

export type Config = typeof(Config)

return table.freeze(Config)
